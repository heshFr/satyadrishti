# Backend API URL (set in Vercel dashboard for production)
VITE_API_BASE=http://localhost:8000

# WebSocket URL (optional — auto-derived from VITE_API_BASE if not set)
# VITE_WS_BASE=ws://localhost:8000
