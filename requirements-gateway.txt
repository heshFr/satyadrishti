# Satya Drishti — API Gateway (Render free tier, 512MB RAM)
# No torch/transformers — all ML inference is forwarded to HF Spaces worker.

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database (PostgreSQL via Neon.tech)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.20.0  # fallback for local dev
alembic>=1.13.0    # schema migrations

# Auth
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0
passlib>=1.7.4

# HTTP client (forwards to inference worker)
httpx>=0.27.0

# Validation & utils
pydantic>=2.0.0
python-multipart>=0.0.9
